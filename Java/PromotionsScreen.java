package inncontrol.promotions;

import javafx.scene.layout.VBox;
import javafx.scene.control.Label;

public class PromotionsScreen extends VBox {
    public PromotionsScreen() {
        Label label = new Label("PromotionsScreen loaded.");
        this.getChildren().add(label);
    }
}
