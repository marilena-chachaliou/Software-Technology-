package inncontrol.reviews;

import javafx.scene.layout.VBox;
import javafx.scene.control.Label;

public class GuestReviewsScreen extends VBox {
    public GuestReviewsScreen() {
        Label label = new Label("GuestReviewsScreen loaded.");
        this.getChildren().add(label);
    }
}
